import Layout from "../components/layouts/Layout";
import CreateUpload from "../CreateUpload";

interface Props {}

const CreateUploadPage = ({}: Props) => {
  return (
    <Layout>
      <CreateUpload />
    </Layout>
  );
};
export default CreateUploadPage;
